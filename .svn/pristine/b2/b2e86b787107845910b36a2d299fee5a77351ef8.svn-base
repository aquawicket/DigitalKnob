### INSTALL ###
IF(NOT WIN32)
	RETURN()
ENDIF()

IF(NOT EXISTS ${3RDPARTY}/upx391w)
	DKSETPATH(${DIGITALKNOB}/Download)
	DKDOWNLOAD(http://digitalknob.com/Download/Tools/upx391w.zip)
	DKEXTRACT(${DIGITALKNOB}/Download/upx391w.zip ${3RDPARTY})
ENDIF()