IF(WIN)
	RETURN()
ENDIF()
IF(LINUX)
	RETURN()
ENDIF()
IF(ANDROID)
	RETURN()
ENDIF()
IF(MAC)
	RETURN()
ENDIF()

DKDEPEND(DKAudio)
DKDEPEND(DKSDLWindow)
DKDEPEND(SDL2_mixer-2.0.1)

DKPLUGIN(DKSDLAudio)
