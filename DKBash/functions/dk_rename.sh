# dk_include_guard()

##################################################################################
# dk_rename(<from> <to>)
#
#
dk_rename () {
	dk_verbose "dk_rename($*)"
	[ $# -ne 2 ] && dk_error "Incorrect number of parameters"
	
	#TODO
	#[ ? = "success" ]
}