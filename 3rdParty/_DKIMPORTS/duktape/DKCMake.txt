### VERSION ###
DKSET(DUKTAPE_VERSION duktape-2.6.0)

### INSTALL ###
DKINSTALL(https://duktape.org/${DUKTAPE_VERSION}.tar.xz ${DUKTAPE_VERSION})
DKSET(DUKTAPE ${3RDPARTY}/${DUKTAPE_VERSION})


### LINK ###
IF(WIN)
	DKDEFINE(DUK_F_VBCC)
ENDIF()
IF(ANDROID)
	DKDEFINE(DUK_F_32BIT_PTRS)
ENDIF()