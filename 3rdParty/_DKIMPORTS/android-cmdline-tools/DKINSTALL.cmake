#!/usr/bin/cmake -P
### DK.cmake ############################################################
if(NOT EXISTS "$ENV{DKCMAKE_FUNCTIONS_DIR_}DK.cmake")
	cmake_policy(SET CMP0009 NEW)
	file(GLOB_RECURSE DK.cmake "/DK.cmake")
	list(GET DK.cmake 0 DK.cmake)
	get_filename_component(DKCMAKE_FUNCTIONS_DIR "${DK.cmake}" DIRECTORY)
	set(ENV{DKCMAKE_FUNCTIONS_DIR_} "${DKCMAKE_FUNCTIONS_DIR}/")
endif()
include("$ENV{DKCMAKE_FUNCTIONS_DIR_}DK.cmake")
include_guard()
#########################################################################


############ android-cmdline-tools ############

dk_depend(android-sdk)
#dk_depend(openjdk-8)

#dk_validate(Host_Os "dk_Host_Os()")
dk_getFileParams("${CMAKE_CURRENT_LIST_DIR}/dkconfig.txt")

if(Windows_Host)
	dk_import(${android-cmdline-tools_Windows_Import} 	INSTALL_PATH "${android-sdk}/cmdline-tools/latest")
elseif(Mac_Host)
	dk_import(${android-cmdline-tools_Mac_Import} 		INSTALL_PATH "${android-sdk}/cmdline-tools/latest")
else()
	dk_import(${android-cmdline-tools_Linux_Impot} 		INSTALL_PATH "${android-sdk}/cmdline-tools/latest")
endif()

dk_set(sdkmanager_bat "${android-cmdline-tools}/bin/sdkmanager.bat")




## FIXME
dk_return()

dk_validate(ENV{DK3RDPARTY_DIR} "dk_DK3RDPARTY_DIR()")

if(Windows_Host)
	dk_exec(sdkmanager --sdk_root=${android-sdk} WORKING_DIRECTORY ${ANDROID_CMDLINE_TOOLS}/bin)
	if(NOT EXISTS ${android-sdk}/build-tools/${ANDROID_BUILD_TOOLS_VERSION})
		dk_info("Installing Android build-tools: ${ANDROID_BUILD_TOOLS_VERSION} . . .")
		set(ARGUMNTS "${SDKMANAGER_BAT} --install build-tools\\;${ANDROID_BUILD_TOOLS_VERSION} --sdk_root=${android-sdk}")
		separate_arguments(ARGUMNTS)
		dk_exec(${ARGUMNTS})
	endif()
	if(NOT EXISTS ${android-sdk}/build-tools/${ANDROID_BUILD_TOOLS_VERSION})
		dk_fatal("Failed to install Android build-tools")
	endif()
	if(NOT EXISTS ${android-sdk}/platforms/${ANDROIDPLATFORM_VERSION})
		dk_info("Installing Android platform: ${ANDROIDPLATFORM_VERSION} . . .")
		set(ARGUMNTS "${SDKMANAGER_BAT} --install platforms\\;${ANDROIDPLATFORM_VERSION} --sdk_root=${android-sdk}")
		separate_arguments(ARGUMNTS)
		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
	endif()
	if(NOT EXISTS ${android-sdk}/platforms/${ANDROIDPLATFORM_VERSION})
		dk_fatal("Failed to install Android platform")
	endif()
	if(NOT EXISTS ${android-sdk}/Sources/${ANDROIDSOURCES_VERSION})
		dk_info("Installing Android API sources: ${ANDROIDSOURCES_VERSION}")
		set(ARGUMNTS "${SDKMANAGER_BAT} --install sources\\;${ANDROIDSOURCES_VERSION} --sdk_root=${android-sdk}")
		separate_arguments(ARGUMNTS)
		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
	endif()
	if(NOT EXISTS ${android-sdk}/Sources/${ANDROIDSOURCES_VERSION})
		dk_fatal("Failed to install Android sources")
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/armeabi-v7a)
		dk_info("Installing Android system image: ${ANDROID_SYSTEM_IMAGES_VERSION} armeabi-v7a")
		set(ARGUMNTS "${SDKMANAGER_BAT} --install system-images\\;${ANDROID_SYSTEM_IMAGES_VERSION}\\;google_apis\\;armeabi-v7a --sdk_root=${android-sdk}")
		separate_arguments(ARGUMNTS)
		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/armeabi-v7a)
		dk_fatal("Failed to install Android Image armeabi-v7a")
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/arm64-v8a)
		dk_info("Installing Android API system image: ${ANDROID_SYSTEM_IMAGES_VERSION} arm64-v8a")
		set(ARGUMNTS "${SDKMANAGER_BAT} --install system-images\\;${ANDROID_SYSTEM_IMAGES_VERSION}\\;google_apis\\;arm64-v8a --sdk_root=${android-sdk}")
		separate_arguments(ARGUMNTS)
		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/arm64-v8a)
		dk_fatal("Failed to install Android Image arm64-v8a")
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/x86)
		dk_info("Installing Android API system image: ${ANDROID_SYSTEM_IMAGES_VERSION} x86")
		set(ARGUMNTS "${SDKMANAGER_BAT} --install system-images\\;${ANDROID_SYSTEM_IMAGES_VERSION}\\;google_apis\\;x86 --sdk_root=${android-sdk}")
		separate_arguments(ARGUMNTS)
		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/x86)
		dk_fatal("Failed to install Android Image x86")
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/x86_64)
		dk_info("Installing Android API system image: ${ANDROID_SYSTEM_IMAGES_VERSION} x86_64")
		set(ARGUMNTS "${SDKMANAGER_BAT} --install system-images\\;${ANDROID_SYSTEM_IMAGES_VERSION}\\;google_apis\\;x86_64 --sdk_root=${android-sdk}")
		separate_arguments(ARGUMNTS)
		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
	endif()
	if(NOT EXISTS ${android-sdk}/system-images/${ANDROID_SYSTEM_IMAGES_VERSION}/google_apis/x86_64)
		dk_fatal("Failed to install Android Image x86_64")
	endif()
endif()


#if(Mac_Host)
#	if(NOT EXISTS ${android-sdk}/build-tools/${ANDROID_BUILD_TOOLS_VERSION})
#		dk_info("Installing Android build-tools: ${ANDROID_BUILD_TOOLS_VERSION} . . .")
#		set(ARGUMNTS "${android-sdk}/bin/sdkmanager --install build-tools\\;${ANDROID_BUILD_TOOLS_VERSION} --sdk_root=${android-sdk}")
#		separate_arguments(ARGUMNTS)
#		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
#	endif()
#	if(NOT EXISTS ${android-sdk}/platforms/${ANDROIDPLATFORM_VERSION})
#		dk_info("Installing Android platform: ${ANDROIDPLATFORM_VERSION} . . .")
#		set(ARGUMNTS "${android-sdk}/bin/sdkmanager --install platforms\\;${ANDROIDPLATFORM_VERSION} --sdk_root=${android-sdk}")
#		separate_arguments(ARGUMNTS)
#		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
#	endif()
#endif()



#if(Linux_Host)
#	if(NOT EXISTS ${android-sdk}/build-tools/${ANDROID_BUILD_TOOLS_VERSION})
#		dk_info("Installing Android build-tools: ${ANDROID_BUILD_TOOLS_VERSION} . . .")
#		set(ARGUMNTS "${android-sdk}/bin/sdkmanager --install build-tools\\;${ANDROID_BUILD_TOOLS_VERSION} --sdk_root=${android-sdk}")
#		separate_arguments(ARGUMNTS)
#		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
#	endif()
#	if(NOT EXISTS ${android-sdk}/platforms/${ANDROIDPLATFORM_VERSION})
#		dk_info("Installing Android platform: ${ANDROIDPLATFORM_VERSION} . . .")
#		set(ARGUMNTS "${android-sdk}/bin/sdkmanager --install platforms\\;${ANDROIDPLATFORM_VERSION} --sdk_root=${android-sdk}")
#		separate_arguments(ARGUMNTS)
#		dk_exec(${ARGUMNTS} WORKING_DIRECTORY ${android-sdk}/bin)
#	endif()
#endif()
