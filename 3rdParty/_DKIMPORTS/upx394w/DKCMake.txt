### INSTALL ###
IF(NOT WIN32)
	RETURN()
ENDIF()

IF(NOT EXISTS ${3RDPARTY}/upx394w)
	DKSETPATH(${DIGITALKNOB}/Download)
	DKDOWNLOAD(http://digitalknob.com/Download/Tools/upx394w.zip)
	DKEXTRACT(${DIGITALKNOB}/Download/upx394w.zip ${3RDPARTY})
ENDIF()

DKSET(UPX ${3RDPARTY}/upx394w)