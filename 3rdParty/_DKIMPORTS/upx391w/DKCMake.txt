### INSTALL ###
IF(NOT WIN32)
	RETURN()
ENDIF()

IF(NOT EXISTS ${3RDPARTY}/upx392w)
	DKSETPATH(${DIGITALKNOB}/Download)
	DKDOWNLOAD(http://digitalknob.com/Download/Tools/upx392w.zip)
	DKEXTRACT(${DIGITALKNOB}/Download/upx392w.zip ${3RDPARTY})
ENDIF()

DKSET(UPX ${3RDPARTY}/upx392w)