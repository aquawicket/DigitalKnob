if(WIN)
	DKDEPEND(opengl)
endif()

if(MAC)
	DKDEPEND(opengl)
endif()

if(IOS)
	FIND_LIBRARY(opengles_lib OpenGLES)
	if(NOT opengles_lib)
		DKWARN("Could not locate OpenGLES framework")
	endif()
	APPLE_DKLIB(${opengles_lib})
endif()

if(IOSSIM)
	FIND_LIBRARY(opengles_lib OpenGLES)
	if(NOT opengles_lib)
		DKWARN("Could not locate OpenGLES framework")
	endif()
	APPLE_DKLIB(${opengles_lib})
endif()

if(LINUX)
	DKDEPEND(opengl)
endif()

if(RASPBERRY)
	DKDEPEND(opengl)
endif()	

if(ANDROID)
	DKDEFINE(GL_GLEXT_PROTOTYPES)
	ANDROID_DKLIB(GLESv1_CM)
	ANDROID_DKLIB(GLESv2)
endif()
