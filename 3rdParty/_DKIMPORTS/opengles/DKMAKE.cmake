if(WIN)
	dk_depend(opengl)
endif()

if(MAC)
	dk_depend(opengl)
endif()

if(IOS)
	FIND_LIBRARY(opengles_lib OpenGLES)
	if(NOT opengles_lib)
		dk_warn("Could not locate OpenGLES framework")
	endif()
	APPLE_dk_lib(${opengles_lib})
endif()

if(IOSSIM)
	FIND_LIBRARY(opengles_lib OpenGLES)
	if(NOT opengles_lib)
		dk_warn("Could not locate OpenGLES framework")
	endif()
	APPLE_dk_lib(${opengles_lib})
endif()

if(LINUX)
	dk_depend(opengl)
endif()

if(RASPBERRY)
	dk_depend(opengl)
endif()	

if(ANDROID)
	dk_define(GL_GLEXT_PROTOTYPES)
	ANDROID_dk_lib(GLESv1_CM)
	ANDROID_dk_lib(GLESv2)
endif()
