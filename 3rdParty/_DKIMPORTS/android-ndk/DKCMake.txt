### VERSION ###
DKSET(NDK_VERSION android-ndk-r21e)

### INSTALL ###
IF(CMAKE_HOST_WIN32)
	DKINSTALL(https://dl.google.com/android/repository/${NDK_VERSION}-windows-x86_64.zip android-ndk ${NDK_VERSION})
ENDIF()

IF(CMAKE_HOST_APPLE)
	## FIXME
	DKINSTALL(https://dl.google.com/android/repository/${NDK_VERSION}-mac.zip android-ndk ${NDK_VERSION})
ENDIF()

IF(CMAKE_HOST_LINUX)
	## FIXME
	DKINSTALL(https://dl.google.com/android/repository/${NDK_VERSION}-linux.zip android-ndk ${NDK_VERSION})
ENDIF()

DKDKSET(NDK "${3RDPARTY}/${NDK_VERSION}")
