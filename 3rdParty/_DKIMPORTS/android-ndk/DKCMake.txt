IF(WIN32)
	IF(NOT EXISTS "${3RDPARTY}/android-ndk-r10d/ndk-build.cmd")
		DKSETPATH(${DIGITALKNOB}/Download)
		DKDOWNLOAD(${DKONLINE}/Tools/android-ndk-r10d-windows-x86.exe)
		DKSETPATH(${3RDPARTY})
		DKSET(QUEUE_BUILD ON)
		ANDROID_COMMAND(${DIGITALKNOB}/Download/android-ndk-r10d-windows-x86.exe)
	ENDIF()
	##GET_FILENAME_COMPONENT(ANDROID_NDK "${3RDPARTY}/android-ndk-r10d" ABSOLUTE)
ENDIF()

IF(MAC)
	##TODO
ENDIF()

IF(UNIX)
IF(NOT APPLE)
	##TODO - test
	IF(NOT EXISTS ${3RDPARTY}/android-ndk-r10e)
		DKSETPATH(${DIGITALKNOB}/Download)
		DKDOWNLOAD(${DKONLINE}/Tools/android-ndk-r10e-linux-x86_64.zip)
		DKEXTRACT(${DIGITALKNOB}/Download/android-ndk-r10e-linux-x86_64.zip ${3RDPARTY})
	ENDIF()
ENDIF()
ENDIF()
