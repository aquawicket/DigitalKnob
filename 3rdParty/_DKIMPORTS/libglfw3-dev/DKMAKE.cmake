if(NOT CMAKE_HOST_UNIX)
	return()
endif()

### INSTALL ###
if(LINUX)
	DKSET(CURRENT_DIR /usr)
	DKCOMMAND(sudo apt -y install libglfw3-dev)
endif()
if(MAC)
	DKCOMMAND(brew install glfw3)
	DKINCLUDE(/usr/local/include)
	APPLE_LIB(/usr/local/lib/libglfw3.3.dylib)
	
	#FIND_LIBRARY(glfw_lib glfw3)
	#if(NOT glfw_lib)
	#	APPLE_LIB(${glfw_lib})
	#else()
	#	message(FATAL_ERROR "Could not locate glfw framework")
	#endif()
endif()
