if(WIN)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		message(FATAL_ERROR "Could not locate OpenSLES framework")
	endif()
	WIN_LIB(${opensles_lib})
endif()

if(MAC)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		message(FATAL_ERROR "Could not locate OpenSLES framework")
	endif()
	APPLE_LIB(${opensles_lib})
endif()

if(IOS)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		message(FATAL_ERROR "Could not locate OpenSLES framework")
	endif()
	APPLE_LIB(${opensles_lib})
endif()

if(IOSSIM)
	FIND_LIBRARY(opensles_lib OpenSLES)
	APPLE_LIB(${opensles_lib})
endif()

if(LINUX)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		message(FATAL_ERROR "Could not locate OpenSLES framework")
	endif()
	LINUX_LIB(${opensles_lib})
endif()

if(RASPBERRY)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		message(FATAL_ERROR "Could not locate OpenSLES framework")
	endif()
	RASPBERRY_LIB(${opensles_lib})
endif()	

if(ANDROID)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		message(FATAL_ERROR "Could not locate OpenSLES framework")
	endif()
	ANDROID_LIB(${opensles_lib})
endif()