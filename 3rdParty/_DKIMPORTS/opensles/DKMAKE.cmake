if(WIN)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		WIN_LIB(${opensles_lib})
	else()
		message(WARNING "Could not locate OpenSLES framework")
	endif()
endif()

#if(MAC)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		message(FATAL_ERROR "Could not locate OpenSLES framework")
#	endif()
#	APPLE_LIB(${opensles_lib})
#endif()

if(IOS)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		APPLE_LIB(${opensles_lib})
	else()
		message(STATUS "Could not locate OpenSLES framework")
	endif()
endif()

#if(LINUX)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		message(FATAL_ERROR "Could not locate OpenSLES framework")
#	endif()
#	LINUX_LIB(${opensles_lib})
#endif()

#if(RASPBERRY)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		message(FATAL_ERROR "Could not locate OpenSLES framework")
#	endif()
#	RASPBERRY_LIB(${opensles_lib})
#endif()	

if(ANDROID)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		ANDROID_LIB(${opensles_lib})
	else()
		message(WARNING "Could not locate OpenSLES framework")
	endif()
endif()