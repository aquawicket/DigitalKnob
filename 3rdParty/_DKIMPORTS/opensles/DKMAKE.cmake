if(WIN)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		WIN_dk_lib(${opensles_lib})
	else()
		DKWARN("Could not locate OpenSLES framework")
	endif()
endif()

#if(MAC)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		DKERROR("Could not locate OpenSLES framework")
#	endif()
#	APPLE_dk_lib(${opensles_lib})
#endif()

if(IOS)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		APPLE_dk_lib(${opensles_lib})
	else()
		dk_info("Could not locate OpenSLES framework")
	endif()
endif()

#if(LINUX)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		DKERROR("Could not locate OpenSLES framework")
#	endif()
#	LINUX_dk_lib(${opensles_lib})
#endif()

#if(RASPBERRY)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		DKERROR("Could not locate OpenSLES framework")
#	endif()
#	RASPBERRY_dk_lib(${opensles_lib})
#endif()	

if(ANDROID)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		ANDROID_dk_lib(${opensles_lib})
	else()
		DKWARN("Could not locate OpenSLES framework")
	endif()
endif()