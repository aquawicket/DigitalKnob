if(WIN)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		dk_warn("Could not locate OpenSLES framework")		
	endif()
	WIN_dk_lib(${opensles_lib})
endif()

#if(MAC)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		dk_warn("Could not locate OpenSLES framework")
#	endif()
#	APPLE_dk_lib(${opensles_lib})
#endif()

if(IOS)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		dk_warn("Could not locate OpenSLES framework")
	endif()
	APPLE_dk_lib(${opensles_lib})
endif()

#if(LINUX)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		dk_warn("Could not locate OpenSLES framework")
#	endif()
#	LINUX_dk_lib(${opensles_lib})
#endif()

#if(RASPBERRY)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		dk_warn("Could not locate OpenSLES framework")
#	endif()
#	RASPBERRY_dk_lib(${opensles_lib})
#endif()	

if(ANDROID)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(NOT opensles_lib)
		dk_warn("Could not locate OpenSLES framework")
	endif()
	ANDROID_dk_lib(${opensles_lib})
endif()