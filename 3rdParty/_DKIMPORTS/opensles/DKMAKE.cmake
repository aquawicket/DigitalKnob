if(WIN)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		WIN_DKLIB(${opensles_lib})
	else()
		DKWARN("Could not locate OpenSLES framework")
	endif()
endif()

#if(MAC)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		DKERROR("Could not locate OpenSLES framework")
#	endif()
#	APPLE_DKLIB(${opensles_lib})
#endif()

if(IOS)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		APPLE_DKLIB(${opensles_lib})
	else()
		DKINFO("Could not locate OpenSLES framework")
	endif()
endif()

#if(LINUX)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		DKERROR("Could not locate OpenSLES framework")
#	endif()
#	LINUX_DKLIB(${opensles_lib})
#endif()

#if(RASPBERRY)
#	FIND_LIBRARY(opensles_lib OpenSLES)
#	if(NOT opensles_lib)
#		DKERROR("Could not locate OpenSLES framework")
#	endif()
#	RASPBERRY_DKLIB(${opensles_lib})
#endif()	

if(ANDROID)
	FIND_LIBRARY(opensles_lib OpenSLES)
	if(opensles_lib)
		ANDROID_DKLIB(${opensles_lib})
	else()
		DKWARN("Could not locate OpenSLES framework")
	endif()
endif()