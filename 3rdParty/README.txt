*** The DigitalKnob 3rdParty ***
All 3rdParty libraries and tools are gathered from _DKIMPORTS with a DKCMake.txt file

* extra files inside libraries will be copied over as patches, overwritting existing files.
* tools will simply hold a DKCMake.txt script file to install the tool. 
