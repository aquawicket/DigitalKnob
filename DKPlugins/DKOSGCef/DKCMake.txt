IF(NOT USE_DKCef)
	RETURN()
ENDIF()
IF(ANDROID)
   RETURN()
ENDIF()
IF(MAC)
   RETURN()
ENDIF()
IF(LINUX)
	RETURN()
ENDIF()
IF(IOS)
     RETURN()
ENDIF()
IF(IOSSIM)
     RETURN()
ENDIF()

DKDEPEND(DKOSGWindow)
DKDEPEND(DKCef)
DKDEPEND(DKCefChild)
DKDEPEND(DKAssets)

DKPLUGIN(DKOSGCef)
