IF(NOT HAVE_DKCef)
	RETURN()
ENDIF()

IF(ANDROID)
	RETURN()
ENDIF()

IF(IOSSIM)
	RETURN()
ENDIF()

DKDEPEND(DKSDLWindow)
DKDEPEND(DKCef)
DKDEPEND(DKCefChild)
DKDEPEND(DKAssets)

generateCmake(DKSDLCef)
