IF(NOT USE_DKCef)
	RETURN()
ENDIF()
IF(ANDROID)
   RETURN()
ENDIF()
#IF(MAC)
   #RETURN()
#ENDIF()
IF(IOSSIM)
	RETURN()
ENDIF()
#IF(LINUX)
	#RETURN()
#ENDIF()

DKDEPEND(DKSDLWindow)
DKDEPEND(DKCef)
DKDEPEND(DKAssets)

DKPLUGIN(DKSDLCef)
