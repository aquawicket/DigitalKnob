### DEPENDS ###
IF(WIN)
	IF(CMAKE_HOST_WIN32)
		DKDEPEND(VisualStudio)
	ENDIF()
ENDIF()
IF(MAC)
	IF(CMAKE_HOST_APPLE)
		DKDEPEND(Xcode)
	ENDIF()
ENDIF()
IF(LINUX)
	DKDEPEND(libboost-tools-dev)
ENDIF()
IF(RASPBERRY)
	DKDEPEND(libboost-tools-dev)
ENDIF()
IF(ANDROID)
	DKDEPEND(android-ndk)
	IF(CMAKE_HOST_WIN32)
		DKDEPEND(VisualStudio)
		DKDEPEND(mingw32)
		DKDEPEND(msys)
	ENDIF()
ENDIF()