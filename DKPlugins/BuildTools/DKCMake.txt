### DEPENDS ###
IF(WIN)
	DKDEPEND(vc2019)
ENDIF()
IF(LINUX)
IF(NOT RASPBERRY)
	DKDEPEND(libboost-tools-dev)
ENDIF()
ENDIF()
IF(RASPBERRY)
	DKDEPEND(libboost-tools-dev)
ENDIF()
IF(ANDROID)
	IF(CMAKE_HOST_WIN32)
		DKDEPEND(vc2019)
		DKDEPEND(android-ndk-r21e)
		DKDEPEND(mingw32)
		DKDEPEND(msys)
	ENDIF()
ENDIF()