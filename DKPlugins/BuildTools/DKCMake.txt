### DEPENDS ###
IF(WIN)
	DKDEPEND(VisualStudio)
ENDIF()
IF(MAC)
	DKDEPEND(XCode)
ENDIF()
IF(LINUX)
	DKDEPEND(libboost-tools-dev)
ENDIF()
IF(RASPBERRY)
	DKDEPEND(libboost-tools-dev)
ENDIF()
IF(ANDROID)
	DKDEPEND(android-ndk-r21e)
	IF(CMAKE_HOST_WIN32)
		DKDEPEND(VisualStudio)
		DKDEPEND(mingw32)
		DKDEPEND(msys)
	ENDIF()
ENDIF()