### DEPENDS ###
IF(WIN)
	DKDEPEND(vc2019)
ENDIF()
IF(MAC)
	DKDEPEND(XCode)
ENDIF()
IF(LINUX)
	DKDEPEND(libboost-tools-dev)
ENDIF()
IF(RASPBERRY)
	DKDEPEND(libboost-tools-dev)
ENDIF()
IF(ANDROID)
	DKDEPEND(android-ndk-r21e)
	IF(CMAKE_HOST_WIN32)
		DKDEPEND(vc2019)
		DKDEPEND(mingw32)
		DKDEPEND(msys)
	ENDIF()
ENDIF()