IF(ANDROID)
	RETURN()
ENDIF()
IF(IOS)
	RETURN()
ENDIF()
IF(IOSSIM)
	RETURN()
ENDIF()

DKDEPEND(rtmidi)
IF(USE_DKDuktape)
	DKDEPEND(DKDuktape)
ENDIF()
IF(USE_DKCef)
	DKDEPEND(DKCef)
ENDIF()


DKPLUGIN(DKMidi)
DKASSETS(DKMidi)
